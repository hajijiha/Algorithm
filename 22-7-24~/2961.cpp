#include <bits/stdc++.h>
#define ll long long int
	vector <ll> v1;
	vector <ll> v2;
	ll n;
	ll mi=10000000000;
int combination(ll int r)
{
    if(n == r || r == 0) 
        return 1; 
    else 
        return combination(n - 1, r - 1) + combination(n - 1, r);
}
ll cal(ll m){
	ncm
}
using namespace std;
int main(){
	ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(0);
	ll n;
	ll i,j
	vector <ll> v1;
	vector <ll> v2;
	ll a,b;
	for(i=0;i<n;i++){
		cin >> a >> b;
		v1.push_back(a);
		v2.push_back(b);
	}
	for(i=1;i<=n;i++){
		mi=
	}
	

}
